import 'package:flutter/material.dart';

class Complain extends StatelessWidget {
  const Complain({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Comaplian"
        ),
      ),
    );
  }
}
